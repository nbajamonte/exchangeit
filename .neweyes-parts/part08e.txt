g .kicker','#routing-title','#archivi .section-heading > p','.archive-card h3','.archive-card > p','.source-row span','.availability','#come-funziona .kicker','#journey-title','.journey-step h3','.journey-step p','#partner .kicker','#partner-title','.partner-copy > p:not(.kicker)','.partner-status p','.orbit-chip','.access-section .kicker','#access-title','.access-copy > p:not(.kicker)','.glass-panel h3','.glass-panel li','#trust .kicker','#trust-title','#trust .section-heading > p','.trust-grid h3','.trust-grid p','.footer-brand b','.footer-brand p','.footer-note'];
const editableNodes=[]; editableSelectors.forEach(sel=>document.querySelectorAll(sel).forEach(el=>{ if(!editableNodes.includes(el)){ el.dataset.editable='true'; el.dataset.editKey=`edit-${editableNodes.length}`; editableNodes.push(el); } }));
const initialContent=Object.fromEntries(editableNodes.map(el=>[el.dataset.editKey,el.innerHTML]));
const editToggle=document.getElementById('editToggle'), editorActions=document.getElementById('editorActions'), editorStatus=document.getElementById('editorStatus'); let editMode=false;
function applySavedContent(){ const saved=readStore(KEYS.content,{}); editableNodes.forEach(el=>{ if(Object.prototype.hasOwnProperty.call(saved,el.dataset.editKey)) el.innerHTML=saved[el.dataset.editKey]; }); }
function setEditMode(enabled){ editMode=enabled; document.body.classList.toggle('edit-mode',enabled); editToggle.classList.toggle('is-active',enabled); editToggle.textContent=enabled?'✓ Anteprima':'✎ Modifica contenuti'; editorActions.hidden=!enabled; editableNodes.forEach(el=>el.contentEditable=enabled?'true':'false'); editorStatus.textContent=enabled?'Clicca sui testi evidenziati e riscrivili':'Anteprima'; }
function collectEdits(){ return Object.fromEntries(editableNodes.map(el=>[el.dataset.editKey,el.innerHTML])); }
editToggle.addEventListener('click',()=>setEditMode(!editMode));
document.getElementById('saveEditsBtn').addEventListener('click',()=>{ writeStore(KEYS.content,collectEdits()); editorStatus.textContent=allowLocal?'Modifiche salvate nel browser':'Memoria disattivata: usa Esporta HTML'; editToggle.classList.add('edit-flash'); setTimeout(()=>editToggle.classList.remove('edit-flash'),700); });
document.getElementById('resetEditsBtn').addEventListener('click',()=>{ editableNodes.forEach(el=>el.innerHTML=initialContent[el.dataset.editKey]); try{localStorage